# دليل العرض أمام اللجنة — مشروع نِعْمَتِي

دليل تشغيل كامل لعرض النظام: **داشبورد الإدارة على اللابتوب + تطبيق الجمعية + تطبيق المتبرع** — يعمل بالكامل على شبكة محلية، والإنترنت مستخدم فقط للخرائط والإشعارات الفورية.

---

## الفكرة العامة

```
إنترنت ──→ [الراوتر أو تليفون يبث] ──┬── اللابتوب (السيرفر + قاعدة البيانات + الداشبورد)
                                     ├── تليفون المتبرع
                                     └── تليفون الجمعية
```

- كل عمليات التطبيق (تسجيل، طلبات، قبول، تأكيد) تتم **محلياً** على اللابتوب — لا تعتمد على الإنترنت إطلاقاً.
- الإنترنت يخدم فقط: بلاطات الخرائط + إشعارات FCM. لو انقطع، النظام يستمر والعرض يصمد.

---

## المرحلة 1: التحضير (في البيت، قبل يوم العرض)

### 1.1 اعرف عنوان اللابتوب

بعد اتصال اللابتوب بالشبكة التي ستُستخدم يوم العرض، افتح cmd واكتب:

```
ipconfig
```

خذ الرقم **IPv4** تحت `Wireless LAN adapter Wi-Fi` — مثال: `192.168.1.6`

> ⚠️ **قاعدة ذهبية**: عنوان اللابتوب يختلف حسب الشبكة (راوتر المنزل ≠ هوتسبوت التليفون). لازم تعرف العنوان من **داخل نفس الشبكة** التي ستُعرض بها.

### 1.2 اضبط رابط الـ API في التطبيق

افتح `frontend/.env` وضع عنوان اللابتوب:

```
API_BASE_URL=http://192.168.1.6:8000/api/v1
```

### 1.3 ابنِ ملف التطبيق (APK)

```bash
cd frontend
flutter build apk --release
```

الملف الناتج (لا تنسخ غير الـ release):

```
frontend\build\app\outputs\flutter-apk\app-release.apk
```

> كل مرة تتغير فيها قيمة `API_BASE_URL` أو عنوان الشبكة، **لازم إعادة بناء** — الرابط يُطبع داخل التطبيق وقت البناء.

### 1.4 افتح جدار الحماية (مرة واحدة فقط)

PowerShell **كمسؤول**:

```powershell
netsh advfirewall firewall add rule name="Naamati Demo" dir=in action=allow program="C:\laragon\bin\php\php-8.3.33-Win32-vs16-x64\php.exe" profile=any
```

### 1.5 جهّز بيانات العرض التجريبية

- حساب **متبرع** جاهز (بيانات دخول بسيطة تحفظها).
- حساب **جمعية** جاهز ومعتمد من الداشبورد (اعتمده من `http://localhost:8000/admin`).
- فئات طعام مسجّلة، وبضع طلبات قديمة بأنواع مختلفة لتبدو القوائم حية.
- اختبر دورة كاملة مسبقاً: طلب ← قبول ← استلام ← تأكيد ← إكمال التوزيع.

---

## المرحلة 2: يوم العرض — اختر السيناريو

### السيناريو A: يوجد راوتر/واي فاي في مكان العرض ✅ الأسهل

1. وصّل **اللابتوب والتليفونين** كلهم بنفس واي فاي المكان.
2. اعرف عنوان اللابتوب الجديد بـ `ipconfig` (سيختلف عن بيتك غالباً!).
3. **إذا اختلف العنوان** عن المطبوع في الـ APK: حدّث `.env` وأعد البناء (دقيقتان — لهذا جهّز لابتوبك مبكراً ووصل بالشبكة قبل اللجنة).
4. شغّل السيرفر (انظر المرحلة 3) وابدأ العرض.

### السيناريو B: لا يوجد راوتر — تليفون يبث النت

1. فعّل **نقطة الاتصال (Hotspot)** على التليفون الذي فيه باقة بيانات، واتركه على الشاحن.
2. وصّل **اللابتوب والتليفونين الآخرين** بنقطة اتصال التليفون.
3. اعرف عنوان اللابتوب بـ `ipconfig` (سيكون شكله مثل `192.168.43.x`).
4. حدّث `.env` وأعد البناء إذا اختلف العنوان عن المطبوع في الـ APK.
5. شغّل السيرفر وابدأ العرض.

> النداءات بين التليفونين واللابتوب **لا تستهلك باقة البيانات** — فقط الخرائط والإشعارات تستهلك (كمية ضئيلة).
>
> **إن رفض التليفون الوصول للابتوب** (نادر، عزل أجهزة AP Isolation): وصّل تليفون البيانات باللابتوب بكابل USB وفعّل "USB Tethering"، وفعّل Mobile Hotspot في الويندوز ليبث للهاتفين.

---

## المرحلة 3: تشغيل السيرفر

### الطريقة السريعة (الملف الجاهز)

شغّل `start_demo.bat` من جذر المشروع — يفحص قاعدة البيانات ويشغّل السيرفر على كل الشبكات.

### الطريقة اليدوية

```bash
cd backend
php artisan serve --host=0.0.0.0 --port=8000
```

> ⚠️ `--host=0.0.0.0` **إجباري**. بدونها السيرفر يسمع على `127.0.0.1` فقط والتليفون لن يصل إليه مهما فعلت.

### اختبار الـ 30 ثانية الذي ينقذ العرض

من متصفح أي تليفون: `http://<عنوان-اللابتوب>:8000` — لازم تظهر شاشة Laravel.
إذا ظهرت → كل شيء جاهز. إذا لم تظهر → جدول المشاكل أدناه.

---

## المرحلة 4: سيناريو العرض أمام اللجنة

**التجهيز**: لابتوب يعرض الداشبورد `http://localhost:8000/admin` + تليفونان (أو تليفون واحد بميزة Dual App/التطبيقات المزدوجة).

1. **المتبرع ينشئ طلباً**: يفتح التطبيق، يختار فئة الطعام، يحدد الموقع، ويرسل.
2. **الإشعار يرنّ عند الجمعية** فوراً (FCM) — اترك اللجنة تسمع الرنين وشاهد الطلب يظهر في قائمة الجمعية.
3. **الجمعية تقبل الطلب** بمدة الاستلام — إشعار يصل للمتبرع.
4. **الداشبورد على اللابتوب**: أظهر الطلب نفسه ويتغير وضعه لحظياً عند كل خطوة (اقبله من الداشبورد أيضاً لتُظهر قدرة الإدارة).
5. **التسليم والتأكيد المتبادل** بين الطرفين، ثم الجمعية تسجّل أثر التوزيع (عدد العائلات/الأفراد).
6. أشر للداشبورد: سجل التدقيق، المخالفات، الإحصائيات.

> بنسخة Dual App على تليفون واحد: كل ما سبق يعمل، والإشعارات تظهر على الشاشة، لكن التليفونان الحقيقيان أقوى أثراً أمام اللجنة.

---

## جدول حل المشاكل

| المشكلة | السبب | الحل |
|---|---|---|
| التليفون لا يفتح `http://<IP>:8000` بالمتصفح | جدار الحماية | نفّذ قاعدة "Naamati Demo" (خطوة 1.4) وتأكد أن الشبكة "Private" في الويندوز |
| التطبيق يقول "تعذر الاتصال بالخادم" | السيرفر على `127.0.0.1` | أعد تشغيله بـ `--host=0.0.0.0` |
| التطبيق يعمل على بيتك ولا في مكان العرض | العنوان تغيّر (شبكة مختلفة) | `ipconfig` ← حدّث `.env` ← أعد بناء الـ APK |
| الإشعارات الفورية لا تظهر | انقطاع إنترنت أو FCM بطيء أول تشغيل | افتح صفحة الإشعارات داخل التطبيق — محتواها يُجلب من السيرفر المحلي دائماً |
| الخريطة فارغة/سوداء | بلاطات OSM من الإنترنت | اعتمد الدبوس والعنوان اليدوي؛ سجّل الطلبات مسبقاً إن كان النت شكوكاً |
| المتبرع أرسل والجمعية لم ترِ شيئاً | حساب الجمعية غير معتمد أو مستخدم خاطئ | اعتمد الجمعية من الداشبورد وسجّل دخول صحيح |

## خطط الطوارئ

- **خطة B (الشبكة)**: نفق ngrok — لكنه يتطلب إعادة بناء APK برابط ngrok، جهّز نسخة مسبقاً إن أردت.
- **خطة C (بلا إنترنت نهائياً)**: كل التدفقات الأساسية تعمل محلياً؛ فقط الإشعارات المنبثقة والخرائط تتوقف — لا تبِن جزءاً جوهرياً من العرض عليهما.
- **احتفظ بشاحن** لكل تليفون وللابتوب.
